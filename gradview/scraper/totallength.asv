function linelength = totallength(linhas)
    linelength=0;
    
    % linhas diagonais
    for i=1:length(linhas)
        linelength = linelength + abs(linhas{i}(2,2)-linhas{i}(2,1));
    end
    
    % linhas cruzadas
    for i=1:length(linhas)
        for j=1:length(linhas)
            if linhas{i}(1,1)
                
            end
        end
    end
    
    
    % linhas sobrepostas